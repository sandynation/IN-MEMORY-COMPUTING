# EDA and RTL Verification

Selected Verilog/SystemVerilog evaluation, synthesis experiments, and OpenROAD-oriented material.

The files are intentionally source-first. Generated waveforms, packaged binaries, environments, and large result files are excluded. Start with `run_verilogeval.py`, the `synth_work` examples, and the small reference RTL/testbench files.
